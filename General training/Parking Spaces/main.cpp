#include <bits/stdc++.h>
using namespace std;
#define fast ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);

int main() {
   for(int i=0;i<=1000;i++)
   {if(i%10==0)
       cout << endl;
       cout << i << " ";

   }
    return 0;
}
